[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rtl_weatherband"
version = "0.7.0"
description = "Stream NOAA Weather Radio audio from an RTL SDR to multiple Icecast servers."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "csdr-server @ git+https://github.com/maxbaykowski/csdr_server.git@main",
  "easrecorder @ git+https://github.com/maxbaykowski/easrecorder.git@main",
  "json5>=0.9.0",
  "lameenc>=1.8.2",
  "numpy>=1.26",
  "PyOgg>=0.6.14a1",
  "sounddevice>=0.5.0",
  "soxr>=1.1.0",
]

[project.scripts]
rtl_weatherband = "rtl_weatherband.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
rtl_weatherband = ["assets/fallback.wav", "assets/same_test.wav"]
